'use client';

import { NextPage } from 'next';

import style from './page.module.scss';

const EventEdit: NextPage = () => {
  return (
    <>
      <h1 className={style['title']}>この機能はまだ実装中</h1>

      {/* <EventEditContainer handleSubmit={handleSubmit} /> */}
    </>
  );
};

export default EventEdit;
